import React from 'react';

import App from '../App';

/**
 * Legacy Wix-compatible entry point retained for branch merges.
 * It simply renders the new React SPA so existing imports keep working.
 */
const Home = () => <App />;

export default Home;
